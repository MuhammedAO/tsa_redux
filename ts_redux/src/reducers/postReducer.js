import {FETCH_POSTS, CREATE_POST} from '../actions/types'

const initialState = {
    posts:[],
    post:{}
}

export default function(state = initialState, action){
switch (action.type) {
   
    
    default:
     return state;
}
}